<section id="sobre-nosotros" class="section introduction" aria-labelledby="introduction-title">
	<div class="container introduction__inner">
		<h2 id="introduction-title" class="introduction__title">Tartas de autor<br class="introduction__title-break" /> en Las&nbsp;Rozas</h2>

		<p class="introduction__body">
			En mi casa, el postre siempre tuvo<br />
			su momento. Y quizá por eso<br />
			aprendí que siempre hay que dejar<br />
			un huequito para lo que nos hace<br />
			felices.
		</p>
	</div>
</section>

<style>
	.introduction {
		background: var(--catalogue-dark);
		padding-block: clamp(96px, 14vw, 160px);
	}

	.introduction__inner {
		max-width: 400px;
		margin-inline: auto;
		text-align: left;
	}

	@media (min-width: 641px) {
		.introduction__inner { max-width: 460px; }
	}

	@media (min-width: 1280px) {
		.introduction__inner { max-width: 520px; }
	}

	.introduction__title {
		color: var(--cream-hero);
		/* Satoshi isn't loaded in this project; falls back to Poppins Light (300), already loaded. */
		font-family: var(--font-sans);
		font-weight: 300;
		font-size: clamp(28px, 5vw, 42px);
		text-transform: none;
		letter-spacing: 0.03em;
		line-height: 1.25;
	}

	.introduction__title-break {
		display: none;
	}

	@media (max-width: 640px) {
		.introduction__title-break {
			display: block;
		}
	}

	.introduction__body {
		margin-top: 28px;
		/* Montserrat has no 300 face loaded; browser resolves to its lightest loaded weight (500). */
		font-family: "Montserrat", var(--font-sans);
		font-weight: 300;
		font-size: clamp(14px, 0.9vw, 16px);
		line-height: 1.8;
		letter-spacing: 0.01em;
		color: rgba(255, 250, 244, 0.72);
	}
</style>
