<section id="sobre-nosotros" class="section introduction" aria-labelledby="introduction-title">
	<div class="container introduction__inner">
		<h2 id="introduction-title" class="introduction__title">Tartas de autor<br class="introduction__title-break" /> en Las&nbsp;Rozas</h2>

		<p class="introduction__body">
			En mi casa, el postre siempre tuvo<br class="introduction__body-break" />
			su momento. Y quizá por eso<br class="introduction__body-break" />
			aprendí que siempre hay que dejar<br class="introduction__body-break" />
			un huequito para lo que nos hace<br class="introduction__body-break" />
			felices.
		</p>
	</div>
</section>

<style>
	.introduction {
		background: var(--catalogue-dark);
		padding-block: clamp(96px, 14vw, 160px);
	}

	.introduction__inner {
		max-width: 400px;
		margin-inline: auto;
		text-align: left;
	}

	@media (min-width: 641px) {
		.introduction__inner { max-width: 620px; }
	}

	@media (min-width: 1280px) {
		.introduction__inner { max-width: 680px; }
	}

	.introduction__title {
		color: var(--cream-hero);
		/* Satoshi isn't loaded in this project; falls back to Poppins Light (300), already loaded. */
		font-family: var(--font-sans);
		font-weight: 300;
		font-size: clamp(28px, 5vw, 42px);
		text-transform: none;
		letter-spacing: 0.03em;
		line-height: 1.25;
	}

	/* Always shown, on mobile and desktop alike, so the heading reflows into the same two lines. */
	.introduction__title-break {
		display: block;
	}

	.introduction__body {
		margin-top: 28px;
		/* Montserrat has no 300 face loaded; browser resolves to its lightest loaded weight (500). */
		font-family: "Montserrat", var(--font-sans);
		font-weight: 300;
		font-size: clamp(14px, 0.9vw, 16px);
		line-height: 1.8;
		letter-spacing: 0.01em;
		color: rgba(255, 250, 244, 0.72);
	}

	/* Hardcoded breaks reproduce the mobile line lengths; hide them past mobile so the paragraph reflows into the wider desktop column. */
	.introduction__body-break {
		display: none;
	}

	@media (max-width: 640px) {
		.introduction__body-break {
			display: block;
		}
	}

	@media (min-width: 1024px) {
		.introduction {
			padding-block: clamp(48px, 6vw, 80px);
		}

		/* Sized to its content instead of the shared container gutter, so both lines have room to fit in one row. */
		.introduction__inner {
			max-width: none;
			width: max-content;
			padding-inline: 0;
		}

		.introduction__title-break {
			display: none;
		}
	}
</style>
